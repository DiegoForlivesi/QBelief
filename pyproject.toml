[build-system]
requires = ["setuptools>=68", "wheel", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "DecoderBP"
version = "1.0.0"
description = "Decoder belief propagation project"
dependencies = [
    "matplotlib",
    "numpy",
]