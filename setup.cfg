[build]
build_base = src/build

[build_ext]
inplace = 1
build_temp = src/build/temp
build_lib = src/build/lib

[metadata]
name = DecoderBP
version = 1.0.0

[options]
packages = src

[egg_info]
egg_base = src